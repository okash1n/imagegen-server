{
  "name": "imagegen-server-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "pnpm --filter @imagegen/server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
